# Core API (production ERP) — keep lean for Docker builds
fastapi==0.139.0
starlette>=0.46.0
uvicorn[standard]==0.51.0
pydantic>=2.9.0,<3
python-multipart==0.0.32
openpyxl==3.1.5
psycopg[binary]==3.2.3

# Utils
tqdm==4.67.1
SQLAlchemy==2.0.36
urllib3>=2.5.0,<3.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.2.0
python-dotenv==1.0.1
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.28.1
python-json-logger==2.0.7
sentry-sdk==2.19.2
slowapi==0.1.9
redis==5.2.1
pika==1.3.2

# Optional PDF helper (lightweight; used by RAG ingest module if enabled)
pypdf==6.14.2

# RAG / LLM stack is NOT installed by default (heavy + breaks restricted Docker Hub/PyPI).
# Install separately when needed:
#   pip install -r requirements-rag.txt
