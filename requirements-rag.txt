# Optional RAG / LLM dependencies (LangChain 1.x).
# Do not install in production ERP image unless you use app/rag.py / ingestion.
#
#   pip install -r requirements-rag.txt
#
# Install AFTER requirements.txt (core). Loosen pins if PyPI mirror lacks a wheel.

langchain==1.3.13
langchain-core==1.4.9
langchain-openai==1.3.5
langchain-text-splitters==1.1.2
langchain-ollama>=0.3.0
# Let langchain-openai choose a compatible openai release (avoid hard pins that break mirrors)
openai>=1.40.0,<2

# Optional local embeddings / vector stack (very large — prefer CPU wheels)
# huggingface-hub>=0.30.0
# transformers>=4.30.0
# torch>=2.0.0
# chromadb>=1.0.20
# sentence-transformers>=5.1.2
# python-docx==1.2.0
